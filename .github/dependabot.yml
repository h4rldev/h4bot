version: 2
updates:
  - package-ecosystem: "cargo"
    # Files stored in repository root
    directory: "/"
    schedule:
      interval: "daily"
    allow:
      - dependency-name: "all"
        dependency-type: "direct"
